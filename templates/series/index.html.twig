{% extends 'base.html.twig' %}

{% block title %}Séries{% endblock %}

{% block body %}
<div>
    <h1 class="mb-3">Listagem de séries:</h1>

    <a href="{{ path('app_series_form_create') }}" class="btn btn-dark mb-3">Adicionar</a>

    <ul class="list-group">
        {% for serie in series %}
            <li class="list-group-item">{{ serie.name }}</li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
